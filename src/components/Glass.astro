---
---

<div
    class="box box-border w-full h-full rounded-3xl bg-gradient-to-t from-white-100/20 to-white-100/60 backdrop-blur-xl shadow-glass"
>
    <slot />
</div>

<style>
  .box {
  position: relative;
}
.box::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: 24px; 
  padding: 1px; 
  background:linear-gradient(-45deg,rgba(236, 72, 153, 1),rgba(192, 132, 252, 1), white); 
  -webkit-mask: 
     linear-gradient(#fff 0 0) content-box, 
     linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
          mask-composite: exclude; 
}
</style>
